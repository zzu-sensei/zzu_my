<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_week_schedule_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="整周课表"
            android:textColor="#FFFFFF"
            android:textSize="17sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/widget_week_schedule_refresh"
            android:layout_width="44dp"
            android:layout_height="34dp"
            android:backgroundTint="#1C7565"
            android:contentDescription="刷新整周课表"
            android:minHeight="0dp"
            android:minWidth="0dp"
            android:padding="0dp"
            android:text="刷新"
            android:textColor="#FFFFFF"
            android:textSize="11sp" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="5dp"
        android:layout_weight="1">

        <ImageView
            android:id="@+id/widget_week_schedule_grid"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="彩色整周课表"
            android:scaleType="fitXY" />

        <TextView
            android:id="@+id/widget_week_schedule_status"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="打开应用登录后显示整周课程"
            android:textColor="#F5FBF8"
            android:textSize="12sp" />
    </FrameLayout>

    <TextView
        android:id="@+id/widget_week_schedule_meta"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="含实验 · 本机直连"
        android:textColor="#B8D8CE"
        android:textSize="10sp" />
</LinearLayout>
